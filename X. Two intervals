#include <stdio.h>
 
int main() {
    long long a,b,c,d;
    scanf("%lld %lld %lld %lld",&a,&b,&c,&d);
 
    if (a<=c && c<=b)
    {
        if (d>=b)
        {
            printf("%lld %lld",c,b);
        }
        else if (b>=d)
        {
            printf("%lld %lld",c,d);
        }
    }
    
    else if (a>=c && a<=d)
    {
        if (d>=b)
        {
            printf("%lld %lld",a,b);
        }
        else if (d<=b)
        {
            printf("%lld %lld",a,d);
        }    
    }
    
    else
    printf("-1");
        
    return 0;
}
